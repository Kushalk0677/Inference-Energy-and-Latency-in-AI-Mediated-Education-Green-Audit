# Requirements for GPU inference experiments (code/colab/ and code/local/)
# Tested on: NVIDIA T4, Python 3.12, Google Colab Pro+
#
# Install order matters — uninstall the pre-bundled transformers in Colab first:
#   pip uninstall -y transformers
#   pip install -r requirements-gpu.txt

transformers==4.44.2
accelerate>=0.26.0
bitsandbytes>=0.43.0
codecarbon>=2.3.1
torch>=2.1.0
pandas>=2.0.0
